---
name: note-sum
description: 生成或修订适合复盘的实习学习日志、代码学习笔记、日报、周报和任务评分，可输出到 Obsidian 或普通 Markdown 文档。适用于用户要求总结实习期间做了什么、整理代码对话、写日报或周报、评估哪些工作适合写进简历、或修改已有生成笔记的场景。
---

# Note Sum

## 使用前配置

写入任何内容前，先读取用户项目中的笔记模板和任务评分规则。如果用户没有提供项目专用模板，使用本 skill 自带的 `templates/` 作为默认规则。

这个 skill 既可以输出给 Obsidian，也可以直接输出为普通 `.md` 文档。

默认占位符：

- `{NOTES_DIR}`：生成笔记存放目录，用户只需要配置这一个路径。
- `{DATE}`：当前本地日期，格式为 `YYYY-MM-DD`。

示例：

```text
{NOTES_DIR}=D:\notes\frontend-internship\notes
```

如果这些值缺失，先从用户请求和本地上下文中推断。目标路径仍不明确时，只问一个简短确认问题，不要猜测写入位置。

## 请求模式

默认使用学习笔记追加模式。

只有用户明确要求“日报”“今日汇报”“daily report”等内容时，才使用日报模式。

只有用户明确要求“周报”“本周汇报”“weekly report”等内容时，才使用周报模式。

只有用户明确要求“任务评分”“bug 打分”“简历含金量评分”“resume-value scoring”或要求追加到评分表时，才使用任务评分模式。

只有用户明确要求“修改、修正、更新、补全、重写、替换、补充”等操作，并提供日期、标题或其他可定位信息时，才使用修改模式。

## 写入目标

学习笔记和日报默认写入：

`{NOTES_DIR}/{DATE}.md`

周报默认写入：

`{NOTES_DIR}/{DATE}-周报.md`

任务评分默认写入：

`{NOTES_DIR}/任务评分.md`

如果用户的项目模板给出了不同路径，以用户模板为准。

## 写入规则

目标目录不存在时可以创建。

追加模式下，不得修改、重排、截断或重新格式化已有内容，只能追加到文件末尾。

如果目标文件不存在或为空，只写入本次生成的内容块。

如果目标文件已有内容，追加前加分隔线：

```markdown

---

# 新笔记标题
```

每次新追加的学习笔记、日报、周报或评分块都必须以一级标题 `#` 开头，方便 Obsidian 大纲视图浏览。

修改模式下，只能修改被指定的标题块。未被指定的笔记、报告、分隔线、顺序和格式必须保持不变。定位不清楚时，先问一个确认问题，不要写入。

## 来源约束

只能使用当前对话、用户提供的笔记、本地源码、diff、日志或命令输出中真实存在的信息。

不得编造文件名、变量名、接口、业务流程、验证结果、日期、指标或实现决策。

如果上下文不足以生成有复盘价值的笔记或报告，不要写文件；直接说明缺少哪些具体信息。

## 写作风格

优先保证高信息密度、短句、少标题，并保留具体代码或流程锚点。

学习笔记需要说明：

- 问题或目标。
- 设计思路。
- 按模块或文件展开的实现讲解。
- 真实存在的关键代码。
- 值得复盘的要点。

日报和周报要像可以直接读出来的汇报，不要写成正式公文。重点写清做了什么、为什么做、整体思路、当前结果，以及有依据的下一步。

任务评分必须先读取评分规则。评分单位是“实际解决的问题”，不是 Markdown 标题数量或日志篇数。同一问题跨日期连续开发、修复和补充复盘时，只合并评一次。

## 自带模板

用户未提供项目专用模板时，读取这些文件：

- `templates/learning-note-template.md`
- `templates/report-template.md`
- `templates/task-rating-rules.md`

`examples/` 里的文件只作为语气和结构参考。不得把示例中的事实复制到用户真实笔记里。
